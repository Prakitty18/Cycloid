# Requesting user identity

Treat "Ada Lovelace" as the session owner's identity unless the task explicitly indicates otherwise.
The current prompt was submitted by Cycloid user ID 77, not by the session owner.

<skill_directives>
Follow the /deploy skill: run the deploy checklist before pushing.
</skill_directives>

Workspace dependencies were installed with `npm install` before this prompt started.

# Diagnostic Errors From Previous Turn

Fix these errors before continuing with new work.

src/server.ts(12,3): error — Cannot find name 'foo'.
