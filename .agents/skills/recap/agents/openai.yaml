interface:
  display_name: "Recap"
  short_description: "Summarize the current Codex session"
  default_prompt: "Use $recap to summarize what has happened so far in this Codex session, including status, key evidence, verification, open items, and the next action."
