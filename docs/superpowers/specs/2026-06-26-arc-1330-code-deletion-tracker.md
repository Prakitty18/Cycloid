# ARC-1330 — Code Deletion / Fold / Rename Tracker

- **Purpose:** the design spec ([`…-fsm-v4-consolidated-design.md`](./2026-06-26-arc-1330-lifecycle-fsm-v4-consolidated-design.md)) stops _describing_ the mechanisms the FSM replaces. This file records the **actual code, DB, labels, and infra** so they get **deleted in implementation (rollout PR5)** instead of orphaned. Each row: disposition · real path · what replaces it · risk if missed · a `verify` grep that must return **zero** (outside `.claude/worktrees`) when done.
- **Provenance:** grounded against the main checkout by 4 code-grounding agents (98 targets). Latest applied migration **0204**; D1 is **append-only** — every DB `DELETE/FOLD` means a **new** `DROP TABLE`/`DROP COLUMN` (or table-rebuild where an index references the column) **plus** deleting the DAO/code; the historical `.sql` stays as applied history.
- **Status legend:** `[ ]` not started · `[~]` in progress · `[x]` removed & verified. **(CLOSED 2026-07-03 — D-51…D-60 merged; see [Final disposition](#final-disposition--arc-1330-closeout-wave-11-2026-07-03) for the terminal per-area state. The per-row `[ ]` checkboxes below are the original inventory, superseded by the closeout summary.)**

## ⚠️ Read first — three corrections to the original §11/§12, and the KEEP list

1. **The live comment lease is migration `0120`, not `0186`.** Original §11 named `pr_stage_comments.posting_lease_until` (0186) — that table is **already dropped by migration `0189_drop_pr_stage_comments.sql` (ARC-1290)**, so there is **nothing to drop** (it was created in 0186, dropped in 0189; both stay as applied history). The **real, in-use** comment posting/edit-lease CAS is `pr_review_status_comments.posting_lease_until` (migration **0120**) + `review-loop-status-comment-db.ts`. **KEEP it** (design §14): the version CAS guards the DB row, not the GitHub comment API.
2. **QA-internal liveness is OUT OF SCOPE (P2) — delete the verification TTL lock with NO re-home precondition.** `verification_active_locks` / `getVerificationLock` was the ARC-1273 expired-but-present death signal consumed by the verification-liveness watchdog (`durable-object.ts:4867,4934`). v5 P2 ratifies that QA-internal liveness/death-detection is the QA team's responsibility (design §13) and the QA **contract deadline** is the accepted coarse backstop — so the lock is **deleted outright** (no "re-home the liveness signal first" step). Do **not** substitute `session_index.updated_at`; do **not** re-home the lock heartbeat as a spine mechanism.
3. **The "v2 intent gate" does not exist yet** — confirmed absent (no `intentGate`/`intent_state`/`INTENT_GATE` module). "Intent" today is only the `intent-mismatch` needs-work verdict label. The change is _behavioral_ (make `intent-mismatch` non-blocking in the QA verdict), **no module to delete**.

**KEEP (do NOT delete/fold — design §14):** (a) `pr_review_status_comments.posting_lease_until` / `review-loop-status-comment-db.ts` (0120); (b) epoch lease-reclaim crash-recovery + transient-failure retry budget; (c) per-head QA-spawn idempotency anchor; (d) the **GitHub merge/close poll** + the **absent-CI re-poll** (D17 dropped-webhook / no-CI backstops, SF20); (e) `update_branch_queued_at` ARC-1302 carry-forward marker, folded onto the FSM record (SF22); (f) `cron_sweep_cursors` (0200, pagination); (g) the §14-false-positive `verification` strings below; **(h) net-new ARC-1330 observability (design §18): the `pr_coordination_events.metadata`/`dwell_ms` columns + the `cycloid.fsm.*` metrics/DD events — KEEP, and explicitly OUTSIDE the `verification → qa` rename surface (do not fold or rename them; they are net-new, not legacy).** **(i) net-new ARC-1330 latency surface (delta §B4/§B6/DE-3): the per-PR-per-reviewer settle store (`pending → responded | no_show`, persisted across heads) + the first-contact no-show **DO alarm** (per-session, ~10 min default, tunable) — KEEP, net-new FSM observability/latency machinery and explicitly OUTSIDE the `verification → qa` rename surface (do not fold or rename them; net-new, not legacy). The reviewer no-show settle runs on this per-session DO alarm, **not** the `*/5` cron — the cron keeps only cross-session reconciles (the D17 merge/close & absent-CI polls).** _(The v4 "QA-liveness signal (re-homed)" KEEP is **gone** — P2 deletes the lock; see item 2.)_

**FALSE POSITIVES — same word, NOT the VA gate; do NOT rename to `qa-*`:** `PrTemplateSlot "verification"` (PR-body test-plan section, `pr-template.ts`); `behavior_verification_gap` / `post_execution verification_status` telemetry (`datadog-agent-behavior.tf`, prompt/post-exec evidence, not the gate); `ExecutionVerification` / `VerificationStateContext` / `verification-finalizer.ts` (post-exec runtime evidence); `google-site-verification` (`cloudflare-pages.tf` DNS).

> **⛔ SCOPE CHANGE (Jag, 2026-06-26) — NO `verification → qa` RENAME IN THIS WORK.** The rename belongs to the verification/QA owner's refactor (P2: QA is their feature, not ours). **Keep every existing `verification` name** — in the code we touch AND in the new code we write (the FSM record fields, `project()` labels, events, `agent_role`). **Never introduce a `qa-*` name.** Every `RENAME` row below (Area 2, plus the rename refs in Areas 1/4 and the Datadog/Terraform move) is downgraded to **"leave a marker, do not rename"**: at each site drop one comment and move on —
> `// TODO(qa-rename, ARC-1330): keep this 'verification' name for now. The verification→QA rename belongs to the QA owner's refactor — rename then, and delete this comment.`
> The `DELETE`/`FOLD` rows still apply (dead-code removal is ours). So read Area 2 as the **inventory of sites to mark**, not to rename.

---

## Final disposition — ARC-1330 closeout (Wave 11, 2026-07-03)

All deletion/fold waves (**D-51 … D-60**) are merged. The closeout re-ran the Area 1/3/4 `verify:` greps on the merged tip and classified every surviving hit — **no live legacy DECISION site survives in any area**. The full classified proof lives in the closeout PR body. Per-area terminal state:

- **Area 1 — Concurrency → single-writer CAS: DONE (comment-only + KEEP-set).** The dead mechanisms (`verification_active_locks`, `verification-lock-db`, `verification_session_requests`, `verification-scheduler-db`, `pr_mergeability_attempts` [dropped by `0233`, D-54], `review_loop_settled_last` [folded to `pr_coordination_events`]) survive in the grep only as JSDoc/inline comments describing the removal, plus `migration-drop-pr-mergeability-attempts.test.ts` (the value-survival test — KEEP). **KEEP (design §14 / PERMANENT):** the epoch lease/reclaim crash-recovery machinery — `claimReviewLoopEpochForPrompt`, `reservation_token`, `transient_failure_count`, `reclaimStuckReviewLoopEpoch` — reused by the Phase B FSM webhook dispatch (claim-CAS); this is the bulk of the Area 1 grep hits.
- **Area 2 — `verification → qa` rename surface: MARK-ONLY (no rename, by scope decision).** The `// TODO(qa-rename, ARC-1330)` marker is present at each kept site (62 markers across `apps`/`shared`). The rename (incl. the Datadog `@event`/Terraform move and the `agent_role` value) is the QA owner's refactor — out of scope. The Area 2 grep is non-zero by design.
- **Area 3 — Manual-PR removal + DO↔D1 mirror: DONE (deleted); 3 stored-rows-compat reads remain.** Deleted: the `create-pr` route/DO-route/handler/client (D-55), `isCreatePrAvailable`/`canCreatePr`, `auto_create_pr_enabled` + plumbing (col dropped by `0234`, D-56), `isAutoCreatePrEnabled`, the blind `mirror(ReviewLoopDoneState|CycloidDoneStatus|VerificationState)ToIndex` fns, and the pre-publish `blocked_by_verification` decision path (D-57). **STORED-ROWS-COMPAT:** three read-only comparisons of a persisted `publish_status === "blocked_by_verification"` remain — `child-session-db.ts` (`computePhase` feed), `shared/session/phase.ts` (`computePhase`), `shared/session/publish.ts` (`isFailedPublish`). No code writes the value anymore. **Retire when:** no live/queryable `session_index`/`child_session` row still carries `publish_status='blocked_by_verification'` (all pre-D-57 rows aged out) → delete the three compat comparisons. The `0128`/`0175`/`0234` migration files legitimately name the dropped column (append-only history) — never deleted.
- **Area 4 — Collapsed done/ready vocab + label lists + cron decision sites: DONE (decision sites deleted); vocab RE-HOMED onto `project()`.** Deleted (cron DECISION sites; residue is comments-only): `reconcileReviewLoopDoneState`, `setSessionReviewLoopDoneState`, `computeShowDone`, the `readyEligible` cron setter, the `computeReviewLoopRollup` done-claim engine, and `review-loop-done-reconcile.ts` (whole file gone). `deriveCycloidDoneStatus` survives **exported-but-uncalled** as the projection **parity oracle** (`project.ts` re-expresses it; the parity test compares against it). **RE-HOMED (KEEP, design §12 — NOT a surviving decision site):** the `ReviewLoopDoneState = "working"|"done"` VALUE type + `normalizeReviewLoopDoneState` are the FSM `project()` output type + the normalization at the DO / UI / `session_index` mirror-column boundary; `isAwaitingFirstVerificationVerdict`/`verificationApplies` survive as the gate feeding the KEPT `ci.signal(absent)` producer (`review-loop-ci-signal.ts`); `reduceCiState` KEPT (§14). Area 4's greps are therefore **non-zero by design** — the projection VALUE type was re-homed, not deleted. The deployment-plan "grep returns zero" bar is met in SPIRIT (every cron done-state DECISION is gone); the residual hits are the deliberate `project()` KEEP.
- **Scaffolding — DONE:** the `FSM_MODE` kill-switch + shadow/divergence sampler code + the `arcanist.fsm.divergence` metric/monitor are removed (D-60); the closeout pruned the now-dead divergence dashboard widgets (`infra/datadog-fsm.tf`) and scrubbed the ~39 stale live-src `FSM_MODE` kill-switch comments (the only surviving `FSM_MODE` mentions are the three past-tense "removed at D-60" removal-doc comments).

---

## Area 1 — Concurrency mechanisms → single-writer `applyEvent` CAS

| ✓   | Disp     | What                                                              | Path                                                                                                                                                                                                                                                                                         | Replaced by                                                                                                                                                                                                                                                                                            |
| --- | -------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [ ] | DELETE   | `verification_active_locks` table                                 | `migrations/0160_verification_active_locks.sql` (new DROP migration)                                                                                                                                                                                                                         | version CAS. **P2: delete outright — the QA contract deadline is the accepted backstop; no re-home precondition (was D16).**                                                                                                                                                                           |
| [ ] | DELETE   | `verification-lock-db.ts` DAO (whole file)                        | `apps/control-plane-worker/src/session/verification-lock-db.ts:1-145`                                                                                                                                                                                                                        | version CAS; `verificationLockKey` → the `session_id`/`pr_url` key. Also stales `VERIFICATION_LOCK_TTL_MS` (`constants/verification.ts:34`)                                                                                                                                                            |
| [ ] | DELETE   | Lock wrapper + advisory dual-read                                 | `verification-gate.ts` `acquireVerificationLockOrFindHolder:42-76`, `releaseVerificationLockForPr:79-93`, `refreshVerificationLockForPr:100-114`, `findActiveVerificationSession:139-187`                                                                                                    | version CAS (§11 row 2). **KEEP** `checkVerificationRunLimit`/`resolveVerificationRunBaseline` in this file — that's the run-budget guard, fold into the FSM `under_qa_cap` guard, don't delete                                                                                                        |
| [ ] | FOLD     | `verification_session_requests` table (per-head INSERT-OR-IGNORE) | `migrations/0140_verification_session_requests.sql` (+ 2 indexes)                                                                                                                                                                                                                            | "version advanced past this head" via `head_sha`/`qa_head_sha` + events log                                                                                                                                                                                                                            |
| [ ] | FOLD     | `verification-scheduler-db.ts` DAO (whole file)                   | `apps/control-plane-worker/src/session/verification-scheduler-db.ts:1-300` (claim at 60-79); 7 callers in `verification-auto-scheduler.ts` + `prompt-queue.ts:1143`                                                                                                                          | `applyEvent` CAS + version-keyed dedup                                                                                                                                                                                                                                                                 |
| [ ] | FOLD     | `verification-request-sweeper.ts` cron recovery                   | `…/verification-request-sweeper.ts:182`; registered `router.ts:436-442`                                                                                                                                                                                                                      | cron → repair-only (design §16). **Must unregister from `scheduled()` or it queries a dropped table every \*/5**                                                                                                                                                                                       |
| [ ] | FOLD     | Epoch lease/reservation/counter columns                           | `pr_review_response_epochs`: `0114_…:41-46` (+ idx `:58-59`), rebuilt `0122:32-37`, `0156:33-37`                                                                                                                                                                                             | version CAS. **Table-rebuild migration** (`lease_expires_at` is indexed). Epoch sub-machine + `epoch.status` survive; only the concurrency columns fold. **KEEP** counters' semantics → re-express as guards first (D15)                                                                               |
| [ ] | FOLD     | Epoch lease CAS fns                                               | `review-loop-epochs.ts` `claimReviewLoopEpochForPrompt:1268-1297`, `listStuckReviewLoopEpochs:1307-1330`, `reclaimStuckReviewLoopEpoch:1331-1355`, `blockStuckReviewLoopEpoch:1356-1393`, `markReviewLoopEpochContentionDeferred:1671-1691`, `markReviewLoopEpochTransientFailure:1692-1751` | version CAS. **KEEP `reclaimStuck…` as cleaner repair (D15)**; move sweep callers (`review-loop-sweep.ts:1037,1053,1060,3058`) to events or they double-write outside the CAS                                                                                                                          |
| [ ] | RENAME   | `pr_review_response_epochs.wave` col + unique index + ~70 TS refs | `0114:23,52-53` (+ 0122/0156); `review-loop-epochs.ts`                                                                                                                                                                                                                                       | `epoch_generation` (clarity rename, Area 2 surface — **not** §12, which names neither term, N13). Column + index + all refs together (it's in the dedup unique key)                                                                                                                                    |
| [ ] | FOLD     | `pr_mergeability_attempts` table                                  | `0139_pr_mergeability_attempts.sql` + `0197_…update_branch_queued_at.sql`                                                                                                                                                                                                                    | per-PR record attributes + CAS. **KEEP `update_branch_queued_at`** — now homed as a `pr_coordination` field (design §4, §14 KEEP #6, SF22); ARC-1302 carry-forward marker, read at `review-loop-head-change.ts:72`. Folds onto the FSM record, not deleted with the table.                             |
| [ ] | FOLD     | mergeability DAO fns                                              | `review-loop-epochs.ts:1979-2051` (`getPrMergeabilityAttempt`/`recordPrMergeabilityAttempt`/`markPrUpdateBranchQueued`); callers `review-loop-sweep.ts:2435,2450,2487,2495,2557`                                                                                                             | per-PR record + CAS                                                                                                                                                                                                                                                                                    |
| [ ] | FOLD     | DO key `review_loop_settled_last`                                 | `durable-object.ts:7201-7204`                                                                                                                                                                                                                                                                | version-keyed `pr_coordination_events` log. Inside the silent-stall block (`7110-7234`) the FSM fixes                                                                                                                                                                                                  |
| [ ] | DELETE   | Tests pinned to the above                                         | `tests/test_cloudflare/{verification-lock-db,session/verification-gate,session/verification-auto-scheduler,session/verification-request-sweeper,review-loop-epochs,review-loop-mergeability-dao,review-loop-status-comment}.test.ts`                                                         | FSM transition/guard tests. These are the **compile-time tripwire** proving removal — PR5 can't be green if they still import deleted DAOs. **N12: `orphaned-pr-stage-comments.test.ts` is NOT in this set** — it imports none of these DAOs; do not delete it as a tripwire (see the KEEP row below). |
| [ ] | **KEEP** | `pr_stage_comments` (0186) — already dropped, nothing to do       | `migrations/0186_pr_stage_comments.sql` → dropped by `0189_drop_pr_stage_comments.sql` (ARC-1290)                                                                                                                                                                                            | **Already dropped (SF21)** — no new migration needed. **KEEP `tests/test_scripts/orphaned-pr-stage-comments.test.ts`** (N12): it asserts the orphan stays absent, imports no FSM DAO, and is unrelated to the PR5 tripwire. The **live** lease is 0120 (see top).                                      |

`verify:` `grep -rn 'verification_active_locks\|verification-lock-db\|verification_session_requests\|verification-scheduler-db\|claimReviewLoopEpochForPrompt\|reservation_token\|transient_failure_count\|pr_mergeability_attempts\|review_loop_settled_last' apps/control-plane-worker/src tests | grep -v /.claude/worktrees/`

---

## Area 2 — `verification → QA` rename surface

The single most error-prone area: a half-rename fails **closed** (QA child not recognized → publish-gate / runtime-profile / post-exec finalize fail) or silently **zeroes Datadog** (metric def and emitted `@event` must move in lockstep).

| ✓   | Disp   | What                                                                                               | Path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ------ | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [ ] | RENAME | `AgentRole` literal `"verification"` → `"qa"` (~45 `agentRole === "verification"` sites)           | `shared/agent/schema.ts:3`, `shared/agent/constants.ts:38`; sites across `sandbox-bridge/src/bridge.ts`, `post-execution-runner.ts:313,448`, `webhooks/{shared,github}.ts`, `routes/sessions.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| [ ] | RENAME | Runtime profile `verification_ready_runtime` + `VerificationRuntimeMode`                           | `shared/agent/schema.ts:5-6`, `constants.ts:41-42`, `bridge.ts:271,276` → `qa_ready_runtime`. Miss → QA boots without `app_runtime`, can't exercise the app                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| [ ] | RENAME | `VerificationState` union + 6 `verification-*` label strings → projection `qa-*`                   | `shared/session/phase.ts:42-48`; constants `constants/pr-labels.ts:10-15` → `QA_*_LABEL`; **add `qa-needs-work`**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [ ] | FOLD   | `VerificationResult` union + `verificationResultFromAgentVerdict`                                  | `shared/session/phase.ts:52-56` → record attr `qa_verdict`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| [ ] | RENAME | `VerificationVerdict`/`VerificationAgentVerdict` raw types                                         | `shared/types/sandbox.ts:103,105,125`, `shared/publish-decision.ts:33`, `shared/post-execution.ts:7` → `QaVerdict`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [ ] | FOLD   | `verification-state.ts` label-sync/verdict-stamp service (715 ln)                                  | `…/session/verification-state.ts` (`syncVerificationStateLabels…`, `clearVerificationVerdictForHeadChange:613`, `stampVerificationVerdictHeadForHeadChange:680`) → `project()` + spine actions `record_qa`/`clear_qa`/`restamp_qa`/`advance_head`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [ ] | FOLD   | `verification-auto-scheduler.ts` (630 ln) + `schedule_failed`                                      | `…/session/verification-auto-scheduler.ts` → `request_qa` action; failure → `qa.failed` event → `NEEDS_YOU` (committed-before-side-effects, kills the 500-before-persist stall)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| [ ] | DELETE | `verification-liveness.ts` + `verification-teardown.ts` (ARC-1273)                                 | **P2: collapse to the QA contract deadline + `kill_qa` on exit (`qa.stopped`/`qa.failed`/`{QA}` deadline edges, design §10/§13). Do NOT re-home as a spine heartbeat** — QA-internal liveness is QA-team-owned and out of scope. (Was "re-home onto the QA deadline, D16".)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| [ ] | FOLD   | VA PR-comment surface                                                                              | `github/verification-comment.ts`, `verification-comment-marker.ts`, `session/verification-comment-state.ts` → stage-section projection (`project()`/`pr-stage-region.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| [ ] | RENAME | helper modules                                                                                     | `github/verification-pr-context.ts`, `verification-interrupted-result.ts`, `session/verification-routing.ts`, `effective-verification.ts` → `qa-*`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [ ] | FOLD   | `VerificationPolicy` tri-state (disabled/manual/auto)                                              | `shared/agent/schema.ts:7`, `session/verification-policy.ts` → D7: QA always on (no disabled arm)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [ ] | FOLD   | DO↔D1 mirror of `verification_state`/`result`                                                      | `state.ts:1616,1644` (`setSessionVerificationState/Result`), `db.ts:326` (`mirrorVerificationStateToIndex`), `internal-routes.ts:439-464` → projection write under CAS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| [ ] | RENAME | `session_index` cols + partial index value `'verification'`                                        | `0150` (`verification_state`), `0149:4-5` (`WHERE agent_role='verification'`) → new partial-index migration `WHERE agent_role='qa'`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| [ ] | RENAME | gate predicates                                                                                    | `phase.ts:119` `isAwaitingVerificationVerdict`, `review-loop-sweep.ts:537` `isAwaitingFirstVerificationVerdict`, `:1153` `commentEpochsQueued` → spine guards `is_awaiting_approving_verdict`/`comment_reviews_gated_by_verification`/`in_ci_pass` (also Area 4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| [ ] | RENAME | wire types on session-view / websocket                                                             | `shared/types/session-view.ts:22-23,102-103`, `session-websocket.ts:10-11,116-117` → `qaState`/`qaVerdict` (projection)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| [ ] | RENAME | shared VA result/summary types                                                                     | `shared/agent/verification-result.ts`, `shared/verification-summary.ts` (UI `apps/ui/src/types.ts:95`) → `qa-result.ts`/`qa-summary.ts` feeding `inject_findings`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [ ] | RENAME | `MAX_VERIFICATION_RUNS_PER_PR`                                                                     | `constants/verification.ts:4` → qa-named (keep value/mechanism = `under_qa_cap`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| [ ] | RENAME | epoch `source_kind 'verification'`                                                                 | `0156:41` CHECK, `github/pr.ts:1917,2290` → `epoch_trigger 'verificationIntake'` (new CHECK migration)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| [ ] | RENAME | UI verification surfaces                                                                           | `apps/ui/src/hooks/useSessionFeed.ts:12`, `applyFeedDelta.ts:82`, `utils/status-display.ts`, `types.ts:94-95` → qa feed type + §12 projection                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| [ ] | RENAME | **Datadog/Terraform (`@event` rename + Terraform move SEQUENCED across the deploy boundary, N11)** | log-metrics `datadog-log-metrics.tf:775-832` (`cycloid.verification.*`), `:177-213` (the `*_liveness` / `*_teardown` verification log-metrics — **these collapse with the P2 lock deletion, Area 2 above**); monitors `datadog-monitors.tf:524-576` (+ `final_state:verification-exhausted`); SLO `datadog-review-loop-slos.tf:40-55`; dashboard `datadog-review-loop.tf:13,295-612`, `datadog-session-phase.tf:190-206`; code `@event` `observability/review-loop-events.ts:153,197,367`, `bridge.ts:2624`, `timeline-emitter.ts:127,148` → `cycloid.qa.*` / `qa-*`. **N11: do NOT do this as a single lockstep commit** — per `docs/infrastructure.md`/`docs/deployments.md` the Terraform metric/monitor/SLO/dashboard move and the emitted-`@event` code rename race across deploy; sequence them (add new-named metric, ship code emitting both, cut over, drop old). **Keep the `schedule_failed` monitor** (design §10). |

`verify:` `grep -rn 'agentRole.*verification\|VerificationState\|VerificationResult\|verification-\(pending\|in-progress\|done\|skipped\|stopped\|exhausted\)\|cycloid\.verification\|verification_ready_runtime' apps shared infra | grep -v /.claude/worktrees/` _(then manually exclude the §-top false positives)_

---

## Area 3 — Manual-PR removal + DO↔D1 mirror

| ✓   | Disp   | What                                                                              | Path                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ------ | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [ ] | DELETE | Public route `POST /api/sessions/:id/create-pr`                                   | `routes/sessions.ts:2240-2274` (+ imports `:32-33,138`). Dead authed route re-enters publish outside the CAS                                                                                                                                                                                                                                                                                                  |
| [ ] | DELETE | DO internal route `POST /session/create-pr` + types                               | `durable-object.ts:8020-8022`, `internal-routes.ts:723-728,945,374-382`                                                                                                                                                                                                                                                                                                                                       |
| [ ] | DELETE | `handleCreatePrRequest` wrapper                                                   | `publish-service.ts:734-783`, `pr-workflow.ts:74,176-178`. **KEEP** shared `publishSessionResult`/`isPromptPublishable`/`resolvePublishingPromptId` (auto path uses them)                                                                                                                                                                                                                                     |
| [ ] | DELETE | `createSessionPr` client fn                                                       | `state.ts:1502-1532`                                                                                                                                                                                                                                                                                                                                                                                          |
| [ ] | DELETE | `isCreatePrAvailable` + `CREATE_PR_BLOCKED_ERROR`                                 | `shared/session/eligibility.ts:82-88`                                                                                                                                                                                                                                                                                                                                                                         |
| [ ] | DELETE | `canCreatePr` view field + UI                                                     | `shared/types/session-view.ts:144`, `services/session-view.ts:345`, `apps/ui/src/components/PrSection.tsx:142-176`, `SessionDetail.tsx:485-499`, `api/sessions.ts:586-590`                                                                                                                                                                                                                                    |
| [ ] | DELETE | `user_settings.auto_create_pr_enabled` col (orphan; append-only)                  | `0128_…:6` (+ `0175:18`). Users with it OFF would get a branch that never opens a PR — silent stuck session. **Migrate + user-comms in PR3 and exclude OFF sessions from the divergence metric (design §16, SF19)** — D3 makes publish automatic, so OFF users start auto-publishing.                                                                                                                         |
| [ ] | DELETE | `auto_create_pr` settings plumbing (DAO/service/route/bootstrap)                  | `settings/db.ts:29,52,92,105,115-135`, `settings/service.ts:223,264`, `settings/routes.ts:227-228,282`, `services/bootstrap.ts:502`                                                                                                                                                                                                                                                                           |
| [ ] | DELETE | `isAutoCreatePrEnabled` gate + wiring                                             | `durable-object.ts:15372-15389,1380,1425`, `publish-service.ts:586,2729-2734`, `pr-workflow.ts:69-70`, `prompt-queue.ts:385,1320-1323,3914-3938`                                                                                                                                                                                                                                                              |
| [ ] | DELETE | UI auto-create-PR setting + toggle                                                | `apps/ui/src/types.ts:175`, `components/settings/GeneralSettings.tsx:331-332`                                                                                                                                                                                                                                                                                                                                 |
| [ ] | DELETE | `getPrBlockedReason` **publish guard** (P1 reconcile)                             | `pr-body.ts:1154-1159`. **P1: the pre-publish block is dropped — delete the publish-time guard.** The post-code-review `NEEDS_YOU(blocked_reason)` block text (incl. `owner_approval`) is produced by `project()` from the `blocked_reason` enum (design §12), **not** this publish-guard fn — so QA-blocked PRs still carry block text and never silently auto-merge. (Was "re-home to owner_approval, D8".) |
| [ ] | DELETE | `publishStatus='blocked_by_verification'` pre-publish path (P1)                   | `publish-service.ts:372,836-845,1963-1969`, `pr-notifications.ts:330-355`, `child-session-db.ts:264,299`. **P1: publish is never blocked pre-PR — delete this status, do not rename it.** Post-review owner-approval is the `epoch.blocked(owner_approval) → NEEDS_YOU` FSM path projected from `blocked_reason` (§4/§12).                                                                                    |
| [ ] | DELETE | `blocked_by_verification` shared helpers + `PublishStatus` arm + projections (P1) | `shared/session/publish.ts:16-27`, `shared/types/publish.ts`, `phase.ts:278`, `services/session-view.ts:178`, `apps/ui/src/utils/durable-event-dispatch.ts:225-229`, `hooks/session-state/reducer.ts:117`, `PrSection.tsx:96`. **Delete the pre-publish `blocked_by_verification` arm** (P1); the post-review block surfaces via the `blocked_reason` projection, not a `PublishStatus`.                      |
| [ ] | DELETE | DO→D1 blind mirror fns (no version/rank guard — the drift surface)                | `db.ts:298-345` (`mirrorReviewLoopDoneStateToIndex`/`mirrorCycloidDoneStatusToIndex`/`mirrorVerificationStateToIndex`)                                                                                                                                                                                                                                                                                       |
| [ ] | DELETE | DO mirror-wrapper methods                                                         | `durable-object.ts:4675-4682,4684-4688,4825-4845`. **`persistVerificationStateToD1` also arms the ARC-1273 watchdog in a `finally` — delete/collapse that arm with NO re-home precondition (P2; the QA contract deadline is the accepted backstop, consistent with the "Read first" item 2 and design §14).** (Was "re-home the arm first, D16".)                                                             |
| [ ] | —      | "v2 intent gate" — **confirmed absent**, nothing to delete                        | the change is behavioral: make `intent-mismatch` non-blocking in the QA verdict (`shared/types/sandbox.ts:106`, `bridge.ts:406-414`)                                                                                                                                                                                                                                                                          |

`verify:` `rg -n 'create-pr\|isCreatePrAvailable\|canCreatePr\|auto_create_pr_enabled\|isAutoCreatePrEnabled\|blocked_by_verification\|mirror(ReviewLoopDoneState\|CycloidDoneStatus\|VerificationState)ToIndex' apps shared | grep -v /.claude/worktrees/`

---

## Area 4 — Collapsed done/ready vocabularies + label lists + `*/5` cron decision sites

The "`done`=4 things, `ready`=3, 3 disagreeing label lists" surface. Decision sites move to event transitions; the cron keeps only idempotent repair (design §16/D17).

| ✓   | Disp        | What                                                                                                                                                         | Path                                                                                                                                                                                                                                                                         |
| --- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| [ ] | FOLD        | `ReviewLoopDoneState` union + `normalizeReviewLoopDoneState` (the overloaded "done")                                                                         | `shared/session/phase.ts:27,34-38` + ~15 consumers → spine **state**                                                                                                                                                                                                         |
| [ ] | FOLD        | whole cycloid-done aggregate (`deriveCycloidDoneStatus`, `CycloidDone*`, `ARCANIST_DONE_REASON_ORDER`, normalizers)                                       | `shared/session/phase.ts:58-173` + consumers → `cycloid_done` projection                                                                                                                                                                                                    |
| [ ] | FOLD        | `computeReviewLoopRollup`/`computeReviewLoopDoneClaim`/`ReviewLoopRollup*` done-claim engine                                                                 | `services/review-loop-rollup.ts:166-253` → the pure `transition()` fn. **KEEP `reduceCiState`** as the `ci_green` guard helper                                                                                                                                               |
| [ ] | DELETE      | `verificationApplies` / `autoVerifyDisabled` tri-state                                                                                                       | `review-loop-sweep.ts:533-535` → D7 (no disabled arm)                                                                                                                                                                                                                        |
| [ ] | DELETE      | `isAwaitingFirstVerificationVerdict` (misnamed CI-first gate, the starvation bug)                                                                            | `review-loop-sweep.ts:537-542`, `review-loop-done-reconcile.ts:14,72` → the **disposition store** (reviews registered actionable on arrival) + the **CI-settled epoch-1 trigger** (delta §A1/§B2). **No FSM review-queue / `release_queued_reviews`** — that CI-first-queue machinery is net-new-not-built (don't build the queue)                                                                                                                                                                    |
| [ ] | DELETE      | `computeShowDone` (fail-open `review-loop:done` decision)                                                                                                    | `review-loop-sweep.ts:558-565` → projection of `MERGE_READY` (strict conjunction)                                                                                                                                                                                            |
| [ ] | FOLD        | label list #1 `REVIEW_LOOP_LABEL_META`/`RECONCILED_REVIEW_LOOP_LABELS` + reconcilers                                                                         | `review-loop-sweep.ts:1794-1919` → `project(record).labels[]`                                                                                                                                                                                                                |
| [ ] | FOLD        | label list #2 + rank validator (`ALL_VERIFICATION_LABELS`, `highestRankedVerificationLabel`, `labelSetsDiffer`)                                              | `verification-state.ts:37-110` → `project()` (illegal multi-label combos become unrepresentable)                                                                                                                                                                             |
| [ ] | FOLD        | label list #3 (the **disagreeing** one) `SYNCHRONIZE_STALE_REVIEW_LOOP_LABELS`                                                                               | `webhooks/github.ts:2891-2904` → `project()` teardown on `head.changed`                                                                                                                                                                                                      |
| [ ] | RENAME      | label string constants                                                                                                                                       | `constants/pr-labels.ts:3-15` (`review-loop:ci-red→ci-fix-exhausted`, add `qa-needs-work`, `verification-*→qa-*`)                                                                                                                                                            |
| [ ] | DELETE      | **CRON DECISION** `reconcileReviewLoopDoneState`                                                                                                             | `review-loop-sweep.ts:1931-2104` (+ inline `review-loop-done-reconcile.ts:119`) → `ci.signal`/`caught_up`/`qa.*` edges                                                                                                                                                       |
| [ ] | FOLD        | **CRON DECISION** `setSessionReviewLoopDoneState` setter + DO route + handler (the `7169` 500-before-persist stall)                                          | `state.ts:1599-1614`, `internal-routes.ts:428-434,584-588`, `durable-object.ts:7110-7234` → `applyEvent` CAS (commit before side-effects)                                                                                                                                    |
| [ ] | DELETE      | **CRON DECISION** `readyEligible` setter                                                                                                                     | `review-loop-sweep.ts:2081` (proj `pr-stage-region.ts:37,79,147-151`) → `MERGE_READY` state                                                                                                                                                                                  |
| [ ] | DELETE      | **CRON DECISION** merged/closed close (the _decision_ only)                                                                                                  | `review-loop-sweep.ts:2245-2277` → `pr.merged`/`pr.closed` events. **Delete the decision logic, NOT the poll** — the poll itself is KEPT below (SF20).                                                                                                                       |
| [ ] | DELETE      | **CRON DECISION** needs-work re-intake / RLA re-trigger                                                                                                      | `review-loop-sweep.ts:2591-2661` → `qa.app_breaks → REVIEW / inject_findings`                                                                                                                                                                                           |
| [ ] | DELETE      | **CRON DECISION** loop-disabled reset `clearStaleReviewLoopState`                                                                                            | `review-loop-sweep.ts:2676-2692` → D7 (no disabled arm)                                                                                                                                                                                                                      |
| [ ] | DELETE      | inline webhook done-reconcile (2nd caller)                                                                                                                   | `services/review-loop-done-reconcile.ts` (whole file) → `ci.signal(green                                                                                                                                                                                                     | absent)` event |
| [ ] | FOLD        | webhook synchronize head-change reset                                                                                                                        | `webhooks/github.ts:2992-2999` → `head.changed` event (`advance_head`)                                                                                                                                                                                                       |
| [ ] | DELETE      | DO↔D1 done-vocab mirror writers                                                                                                                              | `db.ts:298-307,309-322`                                                                                                                                                                                                                                                      |
| [ ] | FOLD        | DO-side persist/recompute (`persistReviewLoopDoneStateToD1`, `persistCycloidDoneStatusToD1`, `recomputeCycloidDoneStatus`, `cycloid_done_settled` marker) | `durable-object.ts:4675-4713,4771-4808` → `applyEvent` + `project()` + events log                                                                                                                                                                                            |
| [ ] | RETAIN | D1 mirror cols `session_index.review_loop_done_state` / `cycloid_done_*` / `verification_state` | `0142`, `0199`, `0150` — written by `project()` from spine, retained as live session list/detail read path until UI cutover (D-59d re-scope) |
| [ ] | DELETE | DO-SQLite session cols (`verification_state`, `verification_attempt_count`, `verification_max_attempts`) | `session/schema.ts` migrations 124-126 — writer-less AND reader-less (dropped D-59d); `review_loop_done_state` / `cycloid_done_*` DO copies RETAINED (surviving pr-stage-region seed reader) |
| [ ] | RENAME      | epoch `source_kind` classifier (`bot/human/mixed/ci/verification`)                                                                                           | `review-loop-rollup.ts:14,141,215`, `review-loop-epochs.ts:60-102` → `epoch_trigger`                                                                                                                                                                                         |
| [ ] | **KEEP**    | **GitHub merge/close POLL** (dropped-webhook backstop, D17 reconcile #1, SF20)                                                                               | the poll body inside `review-loop-sweep.ts:2245-2277` — only producer of `pr.merged`/`pr.closed` when the webhook is dropped. KEEP the poll; delete only its done-state _decision_ (row above). Design §14 KEEP #4.                                                          |
| [ ] | **KEEP**    | **Absent-CI re-POLL** (no-CI-repo `ci.signal(absent)` producer, D17, SF20)                                                                                   | the CI re-poll that drives the `REVIEW` CI-settled (`absent`) epoch-1 trigger for repos without CI webhooks — only producer of `absent`. KEEP the poll; the inline done-reconcile _decision_ (`review-loop-done-reconcile.ts`, row above) folds to the `ci.signal(absent)` event. Design §14 KEEP #5. |
| [ ] | **KEEP**    | `cron_sweep_cursors` (pagination, not coordination)                                                                                                          | `0200_cron_sweep_cursors.sql` — must **not** be deleted (cleaner still paginates)                                                                                                                                                                                            |

`verify:` `rg -n 'ReviewLoopDoneState\|deriveCycloidDoneStatus\|computeReviewLoopRollup\|computeShowDone\|reconcileReviewLoopDoneState\|setSessionReviewLoopDoneState\|readyEligible\|review-loop-done-reconcile\|isAwaitingFirstVerificationVerdict' apps shared | grep -v /.claude/worktrees/`

---

## Done-criterion

> **CLOSED (2026-07-03).** See **Final disposition — ARC-1330 closeout** at the top for the terminal per-area state. Two refinements to the original bar below, learned at cutover: (1) the Area 4 `verify:` greps are **non-zero by design** — the `ReviewLoopDoneState` done/ready *vocabulary type* + `normalizeReviewLoopDoneState` were **re-homed** as the FSM `project()` output (design §12), not deleted; every cron done-state *decision* is gone (comment-only residue), which is the real bar. (2) Area 3 keeps three `blocked_by_verification` **stored-rows-compat** reads (retirement condition in the Final disposition). "Grep returns zero" holds literally for the deleted DAOs/decision sites and figuratively for the re-homed vocab.

PR5 is complete when **every Area 1/3/4 `verify:` grep returns zero outside `.claude/worktrees/`** (except the explicit KEEP rows) — **the Area 2 grep is EXCLUDED** (it keys on deliberately-retained `verification` names like `agentRole.*verification` and the emitted `verification-*` labels, so it is non-zero by design; instead assert the `// TODO(qa-rename, ARC-1330)` markers are present at each kept site) — the deleted-DAO test files are removed/rewritten (the compile-time tripwire), and the full **KEEP set survives** — the three structural concurrency mechanisms (comment lease 0120, epoch crash-recovery, per-head QA-spawn idempotency anchor) + the D17 merge/close & absent-CI polls + `update_branch_queued_at` + `cron_sweep_cursors` (0200) + the `verification` false-positive strings (design §14, top-of-file KEEP list). **The ARC-1273 verification TTL lock is NOT kept — it is deleted with the QA contract deadline as the backstop (P2).** Each DB drop is a new append-only migration. **No `verification → qa` rename is performed in this work** — those sites carry only `qa-rename` marker comments; the rename itself (the Datadog `@event`/Terraform move and the `agent_role` value included) is the QA owner's job, out of scope here.
